sudo: required
dist: trusty

language: python

python:
  - "3.6"

branches:
  only:
  - master

install:
  - pip install requests
  - pip install bs4
  - pip install pandas
  - pip insatll lxml

script:
  - python ./arrests.py
