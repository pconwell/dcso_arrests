sudo: required
dist: trusty

language: python

python:
  - "3.6"

branches:
  only:
  - master

install:
  - pip install requests
  - pip install bs4
  - pip install pandas
  - pip install lxml

script:
  - python ./arrests.py
